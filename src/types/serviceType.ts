export type ServiceType = {
    id: string;
    serviceTitle: string;
    servicePrice: number;
    serviceInfo: string;
}

export type DataType = {
    id: string;
    service_title: string;
    service_price: number;
    service_info: string;
}
